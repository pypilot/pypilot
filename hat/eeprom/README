This script writes to the pypilot hat eeprom to specify which devices that particular hat is using.

git clone https://github.com/raspberrypi/hats/
edit to make always not ask
cd hats/eepromutils
make
sudo make install
cd ../..

